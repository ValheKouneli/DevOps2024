workflow:
  rules:
    - if: $CI_COMMIT_BRANCH == "project"

stages:
  - test

temp:
  stage: test
  image: docker:latest
  variables:
    DOCKER_HOST: tcp://docker:6982
  tags: 
    - docker-compose 
  before_script:
    - echo $DOCKER_HOST
    - docker --version
    - docker-compose --version
  script:
    - docker-compose build --no-cache
    - docker-compose up -d
